<h1>会社マスタ</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>会社名</th>
      <th>取引先ID</th>
      <th>取引先</th>
    </tr>
  </thead>
  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.id %></td>
        <td><%= company.name %></td>
        <td><%= company.supplier_id %></td>
        <td><%= company.supplier&.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
